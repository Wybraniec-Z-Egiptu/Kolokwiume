package RozneRodzajeKlas;

public record JakisRecord(int a,int b,String c, double d) {
    /*
    Record to prosta klasa z automatycznie wygenerowanym konstrukotrem, geterami, seterami, toString(),hash(),equals()
    W nawiasach w sygnaturze podajemy jako parametry zmienne jakie będą w recordzie.
    */
}
